import React from 'react'

const Messenger = ({handleChange}) => {
  return (
    <div>This is messenger
        <button onClick={handleChange}>change</button>

    </div>
  )
}

export default Messenger